
&НаСервере
Процедура ВыполнитьОчисткуНаСервере()
	РеквизитФормыВЗначение("Объект").ВыполнитьОчисткуНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьОчистку(Команда)
	ВыполнитьОчисткуНаСервере(); 
	ПоказатьПредупреждение(,"Очистка спр. <Доп отчеты и обработки> завершена!");
КонецПроцедуры
