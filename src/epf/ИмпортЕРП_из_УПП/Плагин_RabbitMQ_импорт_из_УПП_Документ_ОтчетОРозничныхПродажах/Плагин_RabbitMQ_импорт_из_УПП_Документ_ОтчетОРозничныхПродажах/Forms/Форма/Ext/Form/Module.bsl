&НаКлиенте
Процедура ЗагрузитьИзJson(Команда)
	Ссылка = ЗагрузитьИзJsonНаСервере();
	Сообщить(НСтр("ru = '"+строка(Ссылка.ОтчетОРозничныхПродажах)+"'"), СтатусСообщения.БезСтатуса);
КонецПроцедуры

&НаСервере
Функция ЗагрузитьИзJsonНаСервере()
	Об = РеквизитФормыВЗначение("Объект");
	Ссылка = Об.ЗагрузитьИзJsonНаСервере(JsonText);
	СсылочныйТип = Ссылка.ОтчетОРозничныхПродажах.Ссылка;
    Возврат Ссылка;
КонецФункции


