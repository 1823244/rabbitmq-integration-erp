
&НаСервере
Функция ВыгрузитьОбъектНаСервере()
	json = РеквизитФормыВЗначение("Объект").ВыгрузитьОбъект(СсылочныйТип);
	return json;
КонецФункции

&НаКлиенте
Процедура ВыгрузитьОбъект(Команда)
	json = ВыгрузитьОбъектНаСервере();
	message(json);
КонецПроцедуры





&НаСервере
Процедура СоздатьТестовыйДокументНаСервере()
	ДокОбк = Документы.ПередачаТоваровМеждуОрганизациями.СоздатьДокумент();
	
	ДокОбк.Дата = ТекущаяДатаСеанса();
	
	Докобк.Организация = справочники.Организации.ОрганизацияПоУмолчанию();
	
	// Товары
	
	ДокОбк.Товары.Добавить();
	
	// ВидыЗапасов
	
	ДокОбк.ВидыЗапасов.Добавить();
	//РасшифровкаПлатежаСКлиентом
	ДокОбк.РасшифровкаПлатежаСКлиентом.Добавить();
	//РасшифровкаПлатежаСПоставщиком
	ДокОбк.РасшифровкаПлатежаСПоставщиком.Добавить();
	//ДополнительныеРеквизиты
	ДокОбк.ДополнительныеРеквизиты.Добавить();
	//ШтрихкодыУпаковок
	ДокОбк.ШтрихкодыУпаковок.Добавить();

	// ФИНАЛ
	
	Докобк.ОбменДанными.Загрузка = Истина;
	ДокОбк.Записать();
	
	СсылочныйТип = ДокОбк.Ссылка;
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьТестовыйДокумент(Команда)
	СоздатьТестовыйДокументНаСервере();
КонецПроцедуры
